alias ls='ls -GFh'
alias bi='bundle install'
alias be='bundle exec'

alias json='python -mjson.tool'
alias flushdns='dscacheutil -flushcache && sudo killall -HUP mDNSResponder'

alias git_debug='export GIT_TRACE_PACKET=1 GIT_CURL_VERBOSE=1 GIT_TRANSPORT_HELPER_DEBUG=1 GIT_DEBUG_SEND_PACK=1 GIT_TRACE=1'

export GOROOT=$HOME/go
export GOPATH=$GOROOT/bin

export PATH="/usr/local/sbin:/usr/local/bin:$HOME/bin:./node_modules/.bin:/usr/local/share/npm/bin:$GOPATH:$PATH:~/bin/arcanist/bin"

export PS1="\W \\$ "

export ARCHFLAGS="-arch x86_64"

# pip should only run if there is a virtualenv currently activated
# export PIP_REQUIRE_VIRTUALENV=true
# # cache pip-installed packages to avoid re-downloading
# export PIP_DOWNLOAD_CACHE=$HOME/.pip/cache
# export WORKON_HOME=$HOME/git/virtualenvs
# . /usr/local/bin/virtualenvwrapper.sh
# 
# syspip(){
#    PIP_REQUIRE_VIRTUALENV="" pip "$@"
# }

export DOCKER_HOST=tcp://192.168.59.103:2375
export JAVA_HOME="$(/usr/libexec/java_home)"
[ -f ~/.aws_access ] && export AWS_ACCESS_KEY="$(cat ~/.aws_access)"
[ -f ~/.aws_secret ] && export AWS_SECRET_KEY="$(cat ~/.aws_secret)"
export EC2_HOME="/usr/local/Cellar/ec2-api-tools/1.6.13.0/libexec"
